# Optional — only needed for local integration tests (test/optional.integration.test.js).
# Copy to .env and fill in real values. Never commit .env to git.

# OpenAI (optional integration test)
OPENAI_API_KEY=sk-your-openai-key-here

# Used in README examples when wiring real SDK clients in your app:
# GROQ_API_KEY=gsk_your-groq-key-here
# DEEPSEEK_API_KEY=sk-your-deepseek-key-here
